#:kivy 1.8
#:import kivy kivy

<DialogTextElement>:
    RstDocument:
        id: textViewer
        size: (root.size[0], root.size[1] * 0.8)

    Button:
        id: nextButton
        size: (root.size[0], root.size[1] * 0.2)